name: Frontend Lint and Format

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  frontend-checks:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Bun
      uses: oven-sh/setup-bun@v1
      with:
        bun-version: latest

    - name: Install dependencies
      working-directory: ./MtdrSpring/frontend  # Adjust path to your frontend directory
      run: bun install

    - name: Run ESLint
      working-directory: ./MtdrSpring/frontend
      run: bun lint

    - name: Check Prettier formatting
      working-directory: ./MtdrSpring/frontend
      run: bunx prettier --check . --config .prettierrc

    # Auto-format and commit if running on push to specific branches
    # - name: Format with Prettier
    #   if: github.event_name == 'push' && github.ref == 'refs/heads/develop'
    #   working-directory: ./MtdrSpring/frontend
    #   run: bunx prettier --write . --config .prettierrc

    # - name: Commit formatting changes
    #   if: github.event_name == 'push' && github.ref == 'refs/heads/develop'
    #   run: |
    #     git config --global user.name 'GitHub Actions'
    #     git config --global user.email 'actions@github.com'
    #     git add .
    #     git diff --quiet && git diff --staged --quiet || git commit -m "Apply prettier formatting [skip ci]"
    #     git push